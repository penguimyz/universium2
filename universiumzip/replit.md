# Universe OS

## Run on Replit

- Install dependencies with `npm install`.
- Start the application with `node server.js` (or `npm start`).
- The web app listens on port `5000`.

The project is a Node.js/Express static web app with proxy and transport routes. No additional environment variables are required for the current setup.